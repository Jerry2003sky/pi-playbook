# 理念与路线

为了让 pi 更好用，需要对它进行一些改造优化。核心理念是**极力减少对原生架构的破坏**。

这份配置在真实使用中逐步探索、迭代出来，每一层都回应具体需求。本文讲清它的演进逻辑：pi 缺什么、按什么逻辑补，以及插件为什么分成几个阶段。

## pi 开箱后的三个缺口

pi 本体很克制：装完只有终端交互、内置文件工具和模型调用，用起来会碰到三个缺口。

第一个是基本能力缺口。没有联网；会话无自动命名，不方便查找。

第二个是体验与效率缺口。find/grep 在大型仓库里又慢又难用；工具输出原样堆进上下文，实际有效内容很少；上下文占用一路线性增长，逼近窗口上限时主模型在满上下文里挣扎，响应变慢、细节丢失。

第三个是架构缺口。pi 默认是单模型单会话的，所有工作都在一个上下文里完成。任务规模变大后，机械执行的部分和决策的部分挤在一起，既贵又乱，还影响了任务质量与执行效率。

## 三条理念

配置里那些零散的选择，背后是三条逻辑。

第一，**贵的模型做决策，便宜的模型做执行**。GPT-6 Astra 负责推理、架构、审阅这类需要判断力的工作；廉价档模型负责子代理执行、会话命名、剪枝摘要这类机械工作。同一份任务拆给两个档位的模型，总成本远低于全程用主力。这条逻辑贯穿整份配置：pico 子代理钉 zai 的 glm-5.3-flash，会话命名钉 codex 的 gpt-5.6-luna，剪枝摘要钉 akile 的 gpt-5.3-codex-spark——都是各自渠道里最便宜的一档。

第二，**上下文管理**是目前 agent 使用中需要关注的**核心问题**。大量低信息密度的工具输出堆在窗口中，越聊越重，最后只能用 `/compact` 整体压缩。

除 Codex 的服务端压缩外，整体压缩容易造成信息损失。这份配置采用“压低噪声，分级索引”的方式：剪枝引擎把历史工具输出摘要化归档；调研报告写入本地，主会话只收摘要，必要时再取回；低复杂度的速度敏感任务交给子代理执行。这样可以让上下文保持较高的信息密度。

第三，运行状态要可见。pi 在默认配置下可见指标很少：模型速度、上下文占用和缓存命中率都缺少直观视图。

这套配置提供三个仪表：TPS 仪表显示速度，上下文视图显示占用分类，缓存图表显示命中率。会话运行时，每一步消耗在哪里、执行得怎么样，都能直接看到。

## 阶段的划分逻辑

插件分四类放，分类的依据是它们解决的问题和改造的深度。

**基础阶段**解决第一个缺口，补齐最重要的联网能力，以及能优化会话管理的自动命名。这两个装完，pi 基本上实现了功能闭环。

**进阶阶段**解决第二个缺口。fff 解决搜索慢；上下文观察和缓存监控让效率问题变得可见；结构化提问解决交互里的瞎猜。

**高阶阶段**解决第三个缺口，并进一步优化了上下文管理。子代理引入多代理并行和隔离，剪枝让上下文维持高信息密度。这两个对 pi 的行为改造最深，权衡也最明显：剪枝不可避免会打断上下文缓存，带来成本上升，子代理有权限边界等问题。

**界面与观测**单独一类，横跨所有阶段：UI 美化管观感，速度仪表管感知。它们不改变 pi 的行为，也不会增加任务执行的负担，只是让使用过程看得更清楚。

## 这份配置的整体架构

```
主模型（GPT-6 Astra）
  ├─ Agent 工具 → pico 子代理（glm-5.3-flash）：机械执行、报告写盘
  ├─ ask_user_question：需求不明时弹结构化问卷
  └─ 上下文：pi-condense 剪枝（gpt-5.3-codex-spark 摘要）
              ↓ 观测
  pi-context-view（占用图）+ pi-cache-graph（命中率）+ pi-token-speed（速度）
```

这套配置的通用设置通过 `settings.json`（默认模型与插件开关，逐块拆解见 [06-settings.md](06-settings.md)）调整；`models.json`（见 [07-models.md](07-models.md)）负责自定义供应商接入——zenmux 与 akile 两个聚合网关把 Claude Fable 5.1、GPT-5.3 Codex Spark 和 GPT-6 Astra 接进 pi。

自己写的东西有两样：`pico` 子代理（[08-agents.md](08-agents.md)）和全局 AGENTS.md（[09-agents-md.md](09-agents-md.md)）。
