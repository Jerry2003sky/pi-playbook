# 理念与路线

这份配置是在真实使用中长出来的，每一层都对应一个具体的问题。这篇讲清它的骨架：pi 缺什么、我按什么逻辑补、插件为什么分成这几个阶段。

## pi 开箱后的三个缺口

pi 本体很克制：装完只有终端交互、内置文件工具和模型调用。用起来会依次撞到三个缺口。

第一个是基本能力缺口。没有联网，find/grep 在大型仓库里又慢又难用，会话多了连名字都没有，模型响应快慢也感知不到。

第二个是体验与效率缺口。工具输出原样堆进上下文——构建日志几百行里只有错误行有用，测试输出只有汇总行有用；上下文占用一路线性增长，逼近窗口上限时主模型在满上下文里挣扎，响应变慢、细节丢失。

第三个是架构缺口。pi 默认是单模型单会话的，所有工作都在一个上下文里完成。任务规模变大后，机械执行的部分和决策的部分挤在一起，既贵又乱。

## 三条理念

配置里那些零散的选择，背后是三条逻辑。

第一，贵的模型做决策，便宜的模型做执行。deepseek-v4-pro 负责推理、架构、审阅这类需要判断力的工作；flash 类模型负责子代理执行、会话命名、剪枝摘要这类机械工作。同一份任务拆给两个档位的模型，总成本远低于全程用 pro。这条逻辑贯穿整份配置：fast 子代理钉 flash，会话命名钉 flash，剪枝摘要也钉 flash。

第二，上下文是最贵的资源，按库存管理。工具输出堆在窗口里，越聊越贵越慢，最后只能 compact 整体压缩——那是对上下文最粗暴的一种处理。这份配置的做法是分级：剪枝引擎把历史工具输出摘要化归档，调研报告写盘、主会话只收摘要，子代理自带独立上下文。上下文占用保持恒定，长会话才能持续。

第三，运行状态要可见。pi 在默认配置下可见指标很少：模型快不快、上下文被什么占着、缓存命中怎么样，全都看不见。这套配置给运行状态配了三个仪表——速度有 TPS 仪表，上下文占用有分类图，缓存命中率有曲线。它们的主要用途就是可视化本身：会话开着的时候，每一步消耗在哪里、跑得怎么样，随时看得见。

## 阶段的划分逻辑

插件分四类放，分类的依据是它们解决的问题和改造的深度。

**基础阶段**解决第一个缺口，补最底层的两个能力：联网（pi 本体没有，能力从 0 到 1）和会话命名（会话资产化的起点）。这两个装完，pi 从“能用”到“够用”。

**进阶阶段**解决第二个缺口的一半——效率。fff 解决搜索慢；输出压缩和自动压缩解决上下文被噪声和长度拖垮的问题；上下文观察和缓存监控让效率问题变得可见；结构化提问解决交互里的瞎猜。这个阶段的插件相互独立，各自治各自的问题。

**高阶阶段**解决第三个缺口和第二个缺口的另一半——架构级改造。子代理引入多代理并行和隔离，剪枝让上下文占用从线性增长变成近似恒定。这两个对 pi 的行为改造最深，权衡也最明显：剪枝丢历史细节，子代理有权限边界问题。

**界面与观测**单独一类，横跨所有阶段：UI 美化管观感，速度仪表管感知。它们不改变 pi 的行为，也不解决功能缺口，只是让使用过程看得更清楚——界面好不好看、模型快不快，这两个问题从装好 pi 第一天就存在。

## 这份配置的整体架构

```
主模型（deepseek-v4-pro）
  ├─ Agent 工具 → fast 子代理（flash）：机械执行、报告写盘
  ├─ ask_user_question：需求不明时弹结构化问卷
  └─ 上下文：pi-condense 剪枝 + pi-compact-ex 兜底压缩 + pi-rtk-optimizer 输出降噪
              ↓ 观测
  pi-context-view（占用图）+ pi-cache-graph（命中率）+ pi-token-speed（速度）
```

这套配置的通用设置通过 `settings.json`（默认模型与插件开关，逐块拆解见 [06-settings.md](06-settings.md)）调整；`models.json`（思考档位映射，见 [07-models.md](07-models.md)）用于模型的增添与覆盖——新模型发布时 pi 内置的模型目录可能还没跟上，思考强度修正就写在这里。

自己写的东西有两样：`fast` 子代理（[08-agents.md](08-agents.md)）和全局 AGENTS.md（[09-agents-md.md](09-agents-md.md)）。
